<%= form_for(apply_activity) do |f| %>
  <% if apply_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apply_activity.errors.count, "error") %> prohibited this apply_activity from being saved:</h2>

      <ul>
      <% apply_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :open_id %>
    <%= f.text_field :open_id %>
  </div>

  <div class="field">
    <%= f.label :activity_id %>
    <%= f.number_field :activity_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
